- stage: Build
  jobs:
  - job: Build
    pool:
      vmImage: ubuntu-latest 
    steps:
    - script: echo Fake build code

- stage: Deploy
  jobs:
  - job: Deploy
    pool:
      vmImage: ubuntu-latest 
    steps:
    - script: echo Fake deploy code